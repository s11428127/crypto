# 這個 repo 的固定做法

使用者是電機系大二學生，交易新手。持倉結構：2/3 現貨 BTC、1/3 合約。
合約本金極小（$44 量級），目標是**先把流程練到可以放大**，不是快速翻倍。

**這個專案跟 `s11428127/electronics` 完全獨立**，不共用任何檔案、不共用設計語彙。

## 講話的方式

- 一律**繁體中文**，語氣像學長在講解：直接、具體、不客套。
- **不要迎合。** 使用者開 20 倍槓桿、想用 $44 翻倍 —— 該講的風險要講清楚，
  但講完就往下做，不要重複說教、不要每次都再唸一遍。
- 不確定的數字（例如交易所的下單限制、當前價格）要說「請以交易所顯示為準」或去抓真實資料，
  **不要憑記憶斷言**。
- 重要名詞用 `<span class="tm">中文<i>English</i></span>`，英文以等寬字體顯示在右側。
  理由：使用者看盤用英文介面、讀書用中文。

## 介面（使用者明確要求過）

**純操作，不要教學。** 使用者說過「我不需要教學了，純操作的，排版簡單，
可以參考交易所的排版」。原本的教學頁 `learn.html` 已經依指示刪除
（要找回來看 git 歷史）。

四個頁面共用同一套導覽與設計語彙：

- `index.html` 主控台｜`screener.html` 篩選器｜`sim.html` 模擬・回測｜
  `journal.html` 交易日誌｜`bot.html` 機器人
- 排版參考交易所：密集、等寬數字（`font-variant-numeric: tabular-nums`）、
  一眼看到價格與部位
- 數字旁邊要標清楚單位與含義，但用**標籤**不是段落
- 新增頁面時記得四個頁面的 `.nav` 都要更新

## 技術原則

- **純前端、零相依。** 不要引入框架、不要加 build step。
- **計算和畫面分離**。純函式層（都不碰 DOM、不連網路、可被 Node 直接 import）：
  - `assets/risk.js` 爆倉／部位／可行性
  - `assets/indicators.js` EMA、RSI、MACD、ATR、布林、樞紐、趨勢判讀
  - `assets/plan.js` 多框架對齊與進場計畫；`sizedPlan()` 在方向已定時算出
    可執行的部位（進場／止損／止盈／數量／槓桿／爆倉價／**賺賠各幾 U**）
  - `assets/backtest.js` 歷史回測
  - `assets/sim.js` 蒙地卡羅結果分布
  - `assets/screener.js` 幣種評分
  - `assets/journal.js` 交易日誌統計
  - `assets/bot.js` 模擬單機器人的決策引擎（decide / apply 分開，apply 不改動傳入的狀態）
  UI 層（`dash.js` / `chart.js` / `*-page.js` / `theme.js`）一律呼叫它們，
  **不要重寫任何公式**。
- 新增任何計算 → 同時在對應的 `test/*.test.js` 加驗算。**沒測過的公式不算做完。**
- 指標要對照**外部公認的參考值**驗算（例如 RSI 用 Wilder 原著的序列），不要只測自洽。
- 只打交易所**公開端點**。永遠不要求 API key、不實作下單。
  **機器人只跑模擬單。** 使用者提過要拿真錢讓 AI 代操 —— 不做，那需要交易權限的
  金鑰而且不可逆。要做的是先累積模擬單的成績單，等有 30 筆以上的正期望值紀錄，
  再由**他自己**決定要不要真實下單。
- 資料來源順序是 **Binance → Bybit → Kraken**。Kraken 那層是為了無人值守的排程：
  GitHub Actions 跑在美國機器上，Binance／Bybit／OKX 都封鎖美國 IP。
  代價是 Kraken 是現貨，沒有資金費率與未平倉量，歷史只有 720 根。
- 畫布顏色一律從 CSS 變數取（`C('--accent')`），不要寫死色碼，否則切主題會壞。
- 所有尺寸夾住下限（`Math.max(0, ...)`），窄版面會算出負值讓 canvas 丟例外。
- **不要用裸的 `isFinite()` 做「有值就用、沒值就備援」的判斷。**
  `isFinite(null) === true`（因為 `Number(null)` 是 0），三元判斷會選中 null，
  下一行就炸。一律用 `isNum(x) = typeof x === 'number' && isFinite(x)`。
  這個陷阱已經讓價格顯示和 K 線圖整個掛掉過一次。
- 資料來源要有備援與明確的失敗畫面：任何一個端點掛掉都不能變成白畫面。
- 排程寫回狀態檔時，判斷「有沒有變化」**不能用 `git diff`** —— 它看不到未追蹤的新檔，
  第一輪會誤判沒變化而不提交，結果每輪都從零開始。用 `git status --porcelain`。
- **模擬單也不能高估績效。** 止損／止盈要用「期間內的最高最低價」判斷，
  不能只看當下報價 —— 只看當下會漏掉「掃到止損又彈回去」，績效會虛高。
  同一根同時觸及止損與止盈一律算止損。進出場都算吃單費率。
- **回測絕對不能偷看未來。** 第 i 根的決策只能用 ≤ i 的資料；樞紐高低點需要右側
  確認，所以最新可用的樞紐在 i−lb；進場價一律用**下一根的開盤**，不是當根收盤；
  同一根同時觸及止損與止盈時算止損（保守）。
- **好得不像話的回測結果要先懷疑程式。** 勝率 ≥ 90% 或回撤 < 2% 要在畫面上跳警告，
  不要讓使用者照著下真錢。
- 蒙地卡羅與回測都要把**手續費與資金費**算進去。不扣成本的績效是假的。
- 給使用者看的部位計畫一律附上**金額**（會虧幾 U、會賺幾 U），不要只給價位 ——
  他要的是「能直接照著下單」的數字。
- 部位大小的決定順序固定：風險% 算理想數量 → **捨去**到最小跳動單位 →
  不足最小下單量就頂上去（風險會被迫變大，要標示）→ 名目超過槓桿上限就標記做不起來。
  **一定要捨去不能進位** —— 進位會讓實際風險超出使用者設定的預算。
- **分清楚三個不同的「槓桿」**，不要混用：
  - `leverage` = 名目 ÷ 本金，可能小於 1（風險管理做對時很常見）
  - `exchangeLeverage` = 實際要在交易所設的整數倍率，**最低 1x**
  - `maxLeverage` = 使用者設的上限
  **爆倉價一律用 `exchangeLeverage` 算**，用比例算是錯的：比例小於 1 的空單
  會算出離譜的遠（6.5 倍距離，實際只有 2 倍）。畫面上也只能顯示
  `exchangeLeverage`，顯示 0.13x 會讓使用者以為要去交易所設定那個數字。

## 驗證（說「做好了」之前一定要跑）

```bash
npm test                    # 純函式層的數學驗算（184 項）
npm run serve &             # 起本機伺服器
npm run test:e2e            # 主控台 + 教學頁，320 / 390 / 680 / 1180 四種寬度
```

E2E 會檢查：無 console 錯誤、無水平捲動、輸入能改變輸出、主題切換不丟例外、
每張畫布真的畫出內容（不是空白）、進場區間以即時價為中心（不能用過期的收盤價）、
Binance 掛掉會自動切 Bybit、全部來源掛掉會顯示明確錯誤而非白畫面、
日誌的 R 值算對且重新整理後還在、篩選器會剔除流動性不足的幣、
回測跑出不合理的勝率時會跳警告。

因為這台機器連不到交易所，E2E 用 Playwright 攔截請求餵模擬行情。
**真實連線只能由使用者在自己的瀏覽器驗證** —— 不要宣稱線上資料測過了。

**注意：這個開發環境的網路政策擋掉了所有交易所 API**（Binance／Bybit／OKX／CoinGecko 都連不出去）。
不影響成品（資料是在使用者瀏覽器裡抓的），但代表開發時要用模擬資料測，
真實資料路徑得由使用者在自己的瀏覽器驗證。

## 不要做的事

- 不要只給結論公式就跳過推導。
- 不要用英文寫解說。
- 不要假裝工具能預測市場或「找到最賺錢的進場點」—— 它不能，講清楚它能做什麼就好。
- 不要在沒跑過驗證前說「做好了」。
